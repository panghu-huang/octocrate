{{#each modules}}
{{#if exports_all}}
mod {{ name }};

#[allow(unused_imports)]
pub use {{ name }}::*;

{{else}}
pub mod {{ name }};

{{/if}}
{{/each}}

#[cfg(feature = "pagination")]
mod pagination;

#[cfg(feature = "pagination")]
pub use pagination::*;
